# دليل نشر MikroTik AI Assistant - الجزء الثالث: تأمين التطبيق (SSL/TLS)

يعد تأمين الاتصال بين المستخدم والخادم (HTTPS) أمرًا بالغ الأهمية لحماية بيانات الاعتماد الحساسة (مثل اسم المستخدم وكلمة المرور الخاصة بـ MikroTik) التي يتم إرسالها عبر الموقع. سنستخدم **Certbot** وشهادات **Let's Encrypt** المجانية لتأمين الاتصال.

## 1. تثبيت Certbot

Certbot هي أداة مجانية ومفتوحة المصدر تسهل عملية الحصول على شهادات SSL/TLS من Let's Encrypt وتثبيتها.

```bash
sudo apt install certbot python3-certbot-nginx -y
```

## 2. الحصول على شهادة SSL

سنستخدم Certbot مع مكون Nginx الإضافي الذي يقوم تلقائيًا بتعديل إعدادات Nginx لتشغيل HTTPS.

```bash
sudo certbot --nginx -d your_domain.com -d www.your_domain.com
```

سيطلب منك Certbot إدخال عنوان بريد إلكتروني للمراسلات الطارئة والموافقة على شروط الخدمة. بعد ذلك، سيقوم Certbot بما يلي:

1.  التأكد من أن النطاق (Domain) يشير إلى عنوان IP الخاص بالخادم.
2.  الحصول على الشهادة من Let's Encrypt.
3.  تعديل ملف إعداد Nginx الذي أنشأته في الجزء الثاني ليصبح متوافقًا مع HTTPS (إضافة `listen 443 ssl;` وإعداد مسارات الشهادة).
4.  إعادة توجيه جميع طلبات HTTP (المنفذ 80) إلى HTTPS (المنفذ 443).

## 3. التحقق من التجديد التلقائي

شهادات Let's Encrypt صالحة لمدة 90 يومًا فقط، ولكن Certbot يقوم بتثبيت مهمة تجديد تلقائي (Cron Job أو Systemd Timer) لضمان تجديد الشهادة قبل انتهائها.

يمكنك اختبار عملية التجديد التلقائي باستخدام الأمر التالي:

```bash
sudo certbot renew --dry-run
```

إذا لم تظهر أي أخطاء، فهذا يعني أن التجديد التلقائي يعمل بشكل صحيح.

## 4. إعداد جدار الحماية (Firewall)

لضمان أمان الخادم، يجب تفعيل جدار الحماية (UFW) والسماح فقط بالاتصالات الضرورية.

### الخطوة 4.1: تثبيت وتفعيل UFW

```bash
sudo apt install ufw -y
sudo ufw enable
```

### الخطوة 4.2: السماح بالاتصالات الضرورية

يجب السماح بـ SSH (للاتصال بالخادم)، و HTTP (لإعادة التوجيه)، و HTTPS (للاتصال الآمن):

```bash
sudo ufw allow OpenSSH
sudo ufw allow 'Nginx Full' # يسمح بـ 80 و 443
sudo ufw status
```

## 5. تأمين قاعدة البيانات (SQLite)

في هذا المشروع، استخدمنا قاعدة بيانات SQLite، وهي مناسبة للمشاريع الصغيرة. لزيادة الأمان:

1.  **تأمين ملف قاعدة البيانات:** تأكد من أن ملف قاعدة البيانات (عادةً `sqlite.db`) موجود داخل مجلد المشروع ولا يمكن الوصول إليه مباشرة عبر الويب.
2.  **صلاحيات الملفات:** تأكد من أن المستخدم الذي يقوم بتشغيل Gunicorn (المحدد في ملف الخدمة) هو الوحيد الذي لديه صلاحيات القراءة والكتابة على ملف قاعدة البيانات.

باتباع هذه الخطوات، سيكون لديك موقع ويب آمن وموثوق يعمل على خادم إنتاج، وجاهز للاتصال بأجهزة MikroTik عبر عنوان IP حقيقي.
