# وثيقة متطلبات المنتج وخطة التنفيذ - MikroTik AI Assistant

## 1. الرؤية والأهداف

**الرؤية:** أن نكون المساعد الذكي الأول لمديري شبكات MikroTik حول العالم، مما يجعل إدارة الشبكات أكثر سهولة وكفاءة وأمانًا من خلال التحليل الاستباقي والحلول الذكية.

**الهدف من الإصدار الأولي (MVP):** بناء نظام أساسي قادر على الاتصال الآمن بأجهزة MikroTik، جمع البيانات الحية، وتوفير تنبيهات فورية وحلول مبسطة للمشاكل الشائعة بناءً على تحليل سجلات النظام.

## 2. الجمهور المستهدف

1.  **أصحاب شبكات الإنترنت الصغيرة والمتوسطة (WISP).**
2.  **مديرو تقنية المعلومات في الشركات.**
3.  **الهواة والمستخدمون المتقدمون لأجهزة MikroTik.**

## 3. الميزات الأساسية للإصدار الأولي (MVP)

| الميزة | الوصف | الهدف |
| :--- | :--- | :--- |
| **1. الربط الآمن بالراوتر** | السماح للمستخدم بإدخال بيانات الاتصال (IP, Username, Password) للاتصال بجهاز MikroTik عبر واجهة برمجة التطبيقات (API). | إنشاء قناة اتصال آمنة ومستمرة بين التطبيق والراوتر. |
| **2. لوحة التحكم الحية (Live Dashboard)** | عرض فوري للمؤشرات الحيوية للشبكة: استخدام المعالج، استخدام الذاكرة، حالة الاتصال بالإنترنت، وعدد العملاء المتصلين. | إعطاء المستخدم نظرة سريعة ومبسطة على صحة الشبكة. |
| **3. نظام التنبيهات الأساسي** | إرسال تنبيه فوري للمستخدم في حالات محددة وواضحة (انقطاع الإنترنت، ارتفاع استخدام المعالج إلى مستوى حرج). | تمكين المستخدم من الاستجابة السريعة للمشاكل الكبرى. |
| **4. محلل السجلات البسيط (Simple Log Analyzer)** | يقوم الذكاء الاصطناعي بتحليل سجلات النظام (Logs) للبحث عن رسائل خطأ شائعة وتقديم شرح مبسط وحلول مقترحة لها. | ترجمة السجلات التقنية المعقدة إلى تنبيهات مفهومة وحلول قابلة للتنفيذ. |

## 4. تصميم واجهة المستخدم (UI/UX) - المفهوم الأولي

*   **شاشة تسجيل الدخول والربط:** حقول إدخال لـ IP، اسم المستخدم، وكلمة المرور، مع زر "اتصال".
*   **الشاشة الرئيسية (لوحة التحكم):**
    *   بطاقة حالة الاتصال (أخضر/أحمر).
    *   بطاقات صغيرة لعرض المعالج، الذاكرة، العملاء، ووقت التشغيل (Uptime) برسوم بيانية بسيطة.
    *   قسم "آخر التنبيهات" في الأسفل.
*   **شاشة التنبيهات:** قائمة بالتنبيهات التاريخية مع تفاصيل المشكلة والحل المقترح.

## 5. الخطة التقنية (Technical Stack)

| المكون | التقنية المقترحة | السبب |
| :--- | :--- | :--- |
| **الواجهة الأمامية (Frontend)** | **Flutter** | بناء تطبيق يعمل على أندرويد و iOS من كود برمجي واحد (Cross-platform). |
| **الواجهة الخلفية (Backend)** | **Python (FastAPI)** | الأفضل للذكاء الاصطناعي، و FastAPI يوفر سرعة وأداء عاليين للـ API. |
| **قاعدة البيانات (Database)** | **PostgreSQL** | قوية، موثوقة، ومناسبة لتخزين البيانات الهيكلية والبيانات الزمنية (Time-Series Data) للتحليل. |
| **الاتصال بـ MikroTik** | **Python (RouterOS API)** | استخدام مكتبة Python مخصصة للتعامل مع API الخاص بـ RouterOS. |
| **الذكاء الاصطناعي (AI Model)** | **Rule-Based + Scikit-learn** | البدء بنموذج قائم على القواعد لتصنيف الأخطاء الشائعة في السجلات، ثم التوسع لاحقًا باستخدام التعلم الآلي. |

## 6. خطة التنفيذ المرحلية

(تمثل هذه الخطة المراحل المعتمدة للمشروع)

1.  تحديد المتطلبات وتصميم الهيكل الأولي (PRD & UI/UX) - **مكتمل**
2.  إنشاء بيئة العمل وإثبات المفهوم (PoC) للاتصال بـ MikroTik API
3.  تطوير الواجهة الخلفية (Backend) وتخزين البيانات الأساسي
4.  تدريب نموذج الذكاء الاصطناعي الأولي لتحليل السجلات وتقديم التنبيهات
5.  تطوير واجهة المستخدم (Frontend) ولوحة التحكم الحية (Live Dashboard)
6.  دمج الواجهة الأمامية والخلفية واختبار النظام المتكامل (End-to-End Testing)
7.  إعداد خطة العمل ونموذج تحقيق الدخل (Business Model)
8.  تقديم التطبيق ووثائق المشروع النهائية للمستخدم
